---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import HeroImage from '../assets/main.jpg';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { PROJECT_TAGS } from '../data/projectTags';
import { teamMembers } from '../data/teamMembers';

const posts = (await getCollection('blog'))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 3);

---

<!doctype html>
<html lang="ko">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main class="mx-auto w-full max-w-6xl px-4 py-12">
			<section class="flex flex-col gap-12 py-12 lg:flex-row lg:items-center">
				<div class="flex-grow space-y-6">
					<p class="text-sm font-semibold uppercase tracking-[0.2em] text-muted">IBAS DEV TECH BLOG</p>
					<h1 class="text-3xl font-semibold text-ink sm:text-xl lg:text-2xl">기술, 협업, 그리고 성장. <br />
						IBAS 개발팀이 배우고 경험한 모든 것을 이곳에 남깁니다.</h1>
					<p class="text-lg text-muted">
						실제 프로젝트를 만들며 마주한 문제와 해결 과정, <br /> 그리고 개발팀의 고민을 꾸준히 기록합니다.
						
					</p>
					<div class="flex flex-wrap gap-4">
						<a class="inline-flex items-center justify-center rounded-sm border-2 border-accent bg-accent px-6 py-3 font-semibold text-white transition hover:-translate-y-0.5" href="/blog">
							Blog 보기
						</a>
						<a class="inline-flex items-center justify-center rounded-sm border-2 border-accent/30 bg-white/85 px-6 py-3 font-semibold text-ink transition hover:-translate-y-0.5 hover:bg-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/30 focus-visible:ring-offset-2 focus-visible:ring-offset-white" href="/about">
							개발팀 소개
						</a>
					</div>
				</div>
				<div class="flex-grow text-center hidden lg:block">
					<Image
						class="mx-auto rounded-[12px] shadow-glow"
						src={HeroImage}
						alt="IBAS DEV 팀이 기술 문서를 협업 작성하는 모습을 형상화한 일러스트"
						width={512}
						height={640}
						loading="eager"
					/>
				</div>
			</section>

			<section class="py-8">
				<div class="flex flex-wrap items-center justify-between gap-4">
					<div>
						<h2 class="text-2xl font-semibold text-ink sm:text-3xl">Recent Posts</h2>
						<p class="text-muted">최근에 기록한 이야기들을 모아두었습니다.</p>
					</div>
					<a class="text-sm font-semibold text-accent transition hover:text-accent-light" href="/blog">
						모든 글 보기 →
					</a>
				</div>

				{posts.length ? (
					<ul class="mt-8 space-y-4">
						{posts.map((post) => (
							<li class="rounded-3xl border border-muted-light bg-white/95 p-4 shadow-sm">
								<a class="flex flex-col gap-4 no-underline md:flex-row md:items-stretch" href={`/blog/${post.id}/`}>
									<div class="w-full overflow-hidden rounded-2xl bg-muted-light md:w-40 md:shrink-0">
										{post.data.heroImage ? (
											<Image class="aspect-video h-full w-full object-cover md:aspect-auto" widths={[320, 640, 960]} sizes="(max-width: 768px) 100vw, 160px" src={post.data.heroImage} alt="" />
										) : (
											<div class="flex aspect-video h-full w-full items-center justify-center text-sm text-muted md:aspect-auto">IBAS</div>
										)}
									</div>
									<div class="flex-1 space-y-2">
										<div class="flex flex-wrap items-center gap-3 text-xs font-semibold uppercase tracking-[0.2em] text-muted">
											<FormattedDate date={post.data.pubDate} />
											{post.data.tags?.slice(0, 1).map((tagId) => {
												const tag = PROJECT_TAGS.find((item) => item.id === tagId);
												return tag ? (
													<span
														class:list={[
															'rounded-full px-3 py-1 text-[0.65rem]',
															tag.colorClass ?? 'bg-accent/10 text-accent',
														]}
													>
														{tag.label}
													</span>
												) : null;
											})}
										</div>
										<h3 class="text-lg font-semibold text-ink">{post.data.title}</h3>
										<p class="text-base text-violet-500">by. {post.data.author?.name}</p>
										<p class="text-base text-muted">{post.data.description ?? '이 글에 대한 설명이 곧 업데이트됩니다.'}</p>
									</div>
								</a>
							</li>
						))}
					</ul>
				) : (
					<div class="mt-8 rounded-3xl border border-dashed border-muted-light bg-white/60 p-12 text-center">
						<p class="text-4xl">📝</p>
						<p class="mt-3 text-lg font-semibold text-ink">작성된 글이 아직 없습니다.</p>
						<p class="mt-1 text-sm text-muted">곧 새로운 글로 찾아뵐게요!</p>
					</div>
				)}
			</section>

			<section class="py-8">
				<div class="mb-8">
					<h2 class="text-2xl font-semibold text-ink sm:text-3xl">Introduce Our Team</h2>
					<p class="text-muted">IBAS DEV를 함께 만들어가는 사람들을 소개합니다.</p>
				</div>
				<div class="grid gap-6 md:grid-cols-3">
					{teamMembers.map((member) => (
						<a
							class="group flex flex-col gap-4 rounded-2xl border border-muted-light bg-white/90 p-6 shadow-sm transition hover:-translate-y-1 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/25 focus-visible:ring-offset-2 focus-visible:ring-offset-white"
							href={member.link}
							target="_blank"
							rel="noreferrer"
						>
							<div class="flex items-center gap-4">
								<div class="flex h-12 w-12 items-center justify-center rounded-full bg-ink text-base font-semibold text-white" aria-hidden="true">
									{member.initials}
								</div>
								<div>
									<h3 class="text-lg font-semibold text-ink">{member.name}</h3>
									<p class="text-sm font-medium text-accent">{member.role}</p>
								</div>
							</div>
							<p class="flex-1 text-base text-muted">{member.bio}</p>
							<span class="inline-flex items-center gap-2 text-sm font-semibold text-accent transition group-hover:gap-3">
								GitHub 보기 →
							</span>
						</a>
					))}
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>
